pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                sh 'echo "Pull the new code and restart the server at 3000"'
                sh 'pwd'
            }
        }
    }
}
